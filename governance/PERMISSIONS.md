# PERMISSIONS

Capability does not grant permission.

Use permission scopes:

```text
read
write
execute
deploy
admin
```

and bind them to:

- agent;
- tool;
- environment;
- resource.

Production privileges require explicit policy.
