# AGENT STRATEGY

Select temporary specialist roles only when justified by the task.

The orchestrator owns planning, routing, scheduling, integration, verification, and recovery.

Agents must read their task, retrieve minimum sufficient context, obey permissions, produce expected artifacts, verify results, and report evidence.
